app:
  name: challengePlanet
  display_name: "星轨挑战"
  description: "AI驱动的习惯养成与目标挑战工具"
  app_group: "学习成长"
  icon_url: "icon.svg"
  is_public: true
  workers: 1
  path_prefix: /challengePlanet
  depends_on: ["lion","prompt-manager","beeMemory"]

build:
  python: 3.12
  prepare:
    - python3 -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

run:
  web: python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT} --timeout-keep-alive 30

env:
  PORT: "8610"
  SERVICE_TOKEN: "${SERVICE_TOKEN}"
  LION_NAMESPACE: challengePlanet
  LION_BASE_URL: "http://edge-01:9527"
  UC_BASE_URL: "https://songguokr.com/uc-api"
  UC_APP_KEY: "${UC_APP_KEY}"
  UC_APP_SECRET: "${UC_APP_SECRET}"
  UC_JWT_SECRET: "${UC_JWT_SECRET}"
  UC_ENABLED: "true"
  BEEMEMORY_BASE_URL: "http://edge-01:8700"
  NOTIFY_CENTER_URL: "http://edge-01:8910"
  TZ: "Asia/Shanghai"

secrets:
  - SERVICE_TOKEN
  - UC_APP_KEY
  - UC_APP_SECRET
  - UC_JWT_SECRET

health:
  path: /health
  interval: 30
